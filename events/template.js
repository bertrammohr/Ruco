const Discord = require("discord.js");
const fs = require("fs");

module.exports = {
    eventName: "template",
    disabled: false,
    run: async (client, config, args) => {
        
    }
}